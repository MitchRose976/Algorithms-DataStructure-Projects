const solution = (str) => {
    let result = [];
    for (let i = 0; i < str.length; i++) {
        str[i + 1] === undefined ? result.push(str[i] + "_") : result.push(str[i] + str[i + 1]);
        i++; 
    }
    return result;
}
